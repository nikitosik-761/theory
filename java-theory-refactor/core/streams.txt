map: возвращает 1 другой элемент stream

flatMap: возвращает 0 или больше элементов stream, которые потом объединяются в 1 stream

peek: возвращает 1 тот же элемент stream (может использовать method reference (::))